//Add the following JavaScript code to your website. Place it in a location where the User-ID is available, typically after the user logs in or on every page load if the user is always logged in.


// Assuming `user_id` is a JavaScript variable containing the logged-in user's ID
gtag('config', 'G-XXXXXX', {
  'user_id': user_id
});
